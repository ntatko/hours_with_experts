function deepEqual(obj1, obj2) {
  if (obj1 == obj2) {
    return Boolean(1);
  } else {
    if (obj1.value) {
      // obj1 is a list
      if (obj2.value) {
        // obj2 is a list

      } else {
        // obj2 is a list

      }
    } else {
      if (obj2.value) {
        // obj2 is a list

      } else {
        // obj2 is not a list

      }
    }
  }
}
